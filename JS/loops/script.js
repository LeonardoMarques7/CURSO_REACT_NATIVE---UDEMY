// While(Enquanto)

/* 

var x = 0;

while (x < 10) {
    // Enquanto x for menor que 10 fará esse loop
    document.write("<h2>O valor de x é " + x + "</h2>");
    x++; // Incrementando o valor de x ou adicionado x = x + 1
}

*/

// For(Para)

for (a = 0; a <= 10; a++) {
    // Faça enquanto for menor que 5 fará esse loop
    document.write("<h2>O valor de a é " + a + "</h2>");
}

// Loop + variável
var qtdAlunos = 30;

for (alunos = 0; alunos <= qtdAlunos; alunos++) {
    document.write("<h2>Quantidade de alunos é " + alunos + "</h2>");
}
